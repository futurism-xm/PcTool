# 剪切板置顶后按鼠标位置重选（2026-09-10）

- 根据最新要求替代“鼠标不动时保留原条目”：复制置顶、去重及新记录刷新后立即重新命中鼠标所在行，同时更新选中和详情，不要求鼠标移动。
- 回归在鼠标固定于第二行时连续复制 6 次，验证每次复制当前第二行、置顶后仍选中第二行、详情内容随换位更新且指针坐标不变。保留详情选字期间内容稳定、移出清除选择和滚轮测试。
- 两档构建、Debug 核心与 Debug／Release 原生套件通过；Debug 原生新断言首次失败，独立重跑全套通过，未放宽断言。构建日志前缀为 `out/clipboard-reorder-`。下方关于复制后保留旧条目的说明为历史行为。

# 剪切板悬停选择与平滑详情（2026-09-10）

- 列表鼠标移动事件立即选中条目并显示全文，移出后清除鼠标选择。鼠标未移动时，复制去重置顶不让选中项和详情切换到另一条；保留键盘操作及详情选区拖动。
- 取消 350ms 显示等待，保留离开列表与详情后的 400ms 过渡关闭时间，便于移入气泡选字。详情使用单独的逐像素 alpha 边缘，原生文本区域位于内部，避免颜色键加窗口区域硬裁切产生的锯齿。装饰层不抢焦点、不拦截点击，随详情销毁。
- 原生用例验证移入事件返回时已选中且详情可见、无需点击可 Ctrl+C、复制后稳定条目、移出取消选择；原有三档 DPI、全文选择复制、滚轮、缩放、详情关闭和外部点击测试通过。最初新用例未确保鼠标从区域外移入，修正前置位置后验证通过，未削弱复制稳定性断言。
- 已检查 144 DPI 长详情及短详情截图，圆角和尖角通过透明度过渡贴合背景。DPI 为消息模拟，不代表真实混合 DPI 显示器实测。

# 剪切板详情滚轮与右侧间距（2026-09-10）

- 详情使用自绘滚动条，现明确处理正文、气泡留白及滑块转发的滚轮，通过行滚动同步正文与滑块；不依赖移除系统滚动条后的 EDIT 默认处理。累积细小滚轮增量，遵循系统滚轮行数设置。
- 滑块距深灰主体右边缘缩至 3 DIP，正文与滑块间留 4 DIP；向左显示气泡时避开右侧尖角，文本宽度同步增加。短文本高度测量使用新的正文宽度。
- Debug 核心及原生 CTest 2/2、Release 原生套件与两档构建通过。96／144／192 DPI 消息环境检查边距、上下滚轮、滑块同步、留白滚轮、实际 SendInput 悬停滚轮、选择与焦点保持、全选／选区复制及历史列表不被带动。既有短文本、显隐和关闭回归通过。
- 检查 `clipboard-detail-144.png`：右侧滑块贴近主体边缘，文字没有覆盖滑块。高 DPI 为消息模拟，非真实混合 DPI 显示器；未修改用户系统滚轮设置进行逐档实测。

# 剪切板浮窗样式与居中回归（2026-09-09）

主窗口使用圆角白底、四侧阴影、浅灰圆角条目；移除关闭按钮和原生边框。标题右侧保留数量与本次运行提示。每次打开或重复调用均在鼠标所在显示器工作区居中，预留阴影边距。

阴影与细滚动条移至 `src/shared/ui/popup_controls`，翻译和剪切板共同使用；剪切板模块不依赖翻译模块。主列表和全文详情仅在溢出时显示滚动条，控件显隐不改变正文宽度。保留 Esc、外部点击关闭、详情选字和复制。

| 验证 | 结果 |
|---|---|
| Debug、Release 构建与剪切板／翻译核心 CTest | 通过 |
| 首次显示、移动后再次调用、关闭重开均居中 | 通过 |
| 关闭按钮不存在、无系统粗边框、圆角区域、阴影归属及鼠标透明属性 | 通过 |
| 主列表短内容隐藏滚动条、长列表滑块到末尾、滚轮、Home／End 同步 | 通过 |
| 从第二条向上滚动可回到第一条 | 通过 |
| 详情滑块、滚轮与长文本滚动；短详情不显示滚动条 | 通过 |
| 记录监听、去重、剪贴板占用重试、选区复制、Ctrl+A、退出清空 | 通过 |
| 无边框标题拖动、边缘缩放、重复居中 | 通过 |
| 三档 DPI 消息下连续尺寸调整，与强制重绘截图比较 | 通过，无残留像素差异 |
| 翻译原生窗口、卡片展开、细滚动条、浏览器／记事本取词 | Debug、Release 通过 |

桌面测试必须串行运行：两个构建目录同时执行曾因共享系统剪贴板发生干扰；串行重跑通过。96／144／192 DPI 使用窗口 DPI 消息驱动检查，并非三台真实混合 DPI 显示器；负坐标与真实跨屏拖动未实测。

截图在 `out/clipboard-popup-ui`，其中 `clipboard-shadow-144.png` 包含窗口外围，用于检查四周阴影；`clipboard-detail-144.png` 展示详情细滚动条。

复现：

```powershell
ctest --test-dir out/build/modular -R '^(clipboard_history_core|clipboard_history_ui|translation_core|translation_ui)$' --output-on-failure
# 上述桌面测试结束后，再运行 Release 桌面用例。
cmake-build-release/PcToolClipboardTests.exe ui-test
cmake-build-release/PcToolTranslationTests.exe ui out/clipboard-popup-translation-ui
```

## 仅溢出条目显示详情（2026-09-11）

- 悬停详情按摘要实际绘制字体、DPI 和正文区域宽度判断；共用行内边距，超过宽度或摘要达到截断上限才显示。完整显示的短内容仍立即选中并支持 Ctrl+C，但不创建详情窗口。
- 长条目移到短条目立即关闭旧详情；同一条目随着窗口放宽／缩窄重新判断，不依赖鼠标移动。保留详情滚动、选择复制和跨越间隙的行为。
- Debug 构建通过，clipboard_history_core、clipboard_history_ui 均通过（UI 12.82 秒）。覆盖短内容、被省略的单行、长多行、长短切换、窗口放宽／缩窄、静止鼠标复制重排、三档 DPI、详情滚轮和外部关闭。
- 原有缩放截图比较等待从 120 ms 调至 250 ms，让 DWM 圆角合成稳定；曾观察到仅右下角 7×2 像素差异，未放宽像素比较断言。原生键盘测试保留前台与焦点诊断。
- 日志：out/clipboard-overflow-build.log、out/clipboard-overflow-tests.log、out/clipboard-overflow-package.log。真实混合 DPI 多屏未实测。
